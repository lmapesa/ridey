version: 2

sources:
  - name: dbt_raw_data
    database: chicago_data #dbtdb # Tell dbt to look for the source in the "raw" database
    schema: dbt_raw_data # You wouldn't put your source data in public, would you?
    tables:
      - name: chicago_data